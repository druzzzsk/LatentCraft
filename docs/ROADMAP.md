# LatentCraft — Roadmap

## Концепция

**LatentCraft** — проект по оптимизации молекулярных свойств через навигацию в латентном пространстве.

**Идея:** дискретные химические структуры (SMILES / графы) кодируются в непрерывное латентное пространство с помощью VAE. В этом пространстве можно двигаться в направлении улучшения целевых свойств (logP, QED, SA score) и декодировать результат обратно в валидную молекулу.

**Стратегия проекта:** не обучать архитектуры с нуля, а взять проверенные open-source реализации молекулярных VAE, воспроизвести их результаты, а затем систематически сравнить разные методы оптимизации в их латентных пространствах. На основе этого сравнения — предложить и реализовать собственные улучшения.

**Базовые репозитории:**

| Кодовое имя | Репо | Представление |
|---|---|---|
| SMILES-VAE | `aspuru-guzik-group/chemical_vae` | SMILES (one-hot) |
| JT-VAE | `wengong-jin/icml18-jtnn` | Junction tree |
| SELFIES-VAE | `aspuru-guzik-group/selfies` + пример VAE | SELFIES строки |
| HierVAE | `wengong-jin/hgraph2graph` | Иерархический граф |
| MoLeR | `microsoft/molecule-generation` | Авторегрессивный граф |

**Методы оптимизации для сравнения:**

- Gradient ascent (через property predictor)
- Bayesian Optimization (GP-based, BoTorch)
- Evolutionary strategies (CMA-ES)

---

## Фаза 0 — Инфраструктура проекта

### 0.1 Структура репозитория

Создать структуру директорий, `pyproject.toml` (или `requirements.txt`), базовый README с описанием проекта. Настроить `.gitignore` (данные, чекпоинты, `.venv`).

```
LatentCraft/
├── configs/            # YAML-конфиги экспериментов
├── data/               # скрипты загрузки и предобработки данных
├── models/             # обёртки над внешними VAE
├── optimizers/         # реализации методов оптимизации
├── evaluation/         # метрики, scoring, визуализация
├── experiments/        # скрипты запуска экспериментов
├── notebooks/          # EDA и анализ результатов
├── results/            # логи, чекпоинты, артефакты (gitignored)
├── docs/
└── tests/
```

### 0.2 Единый конфиг-формат

Выбрать формат конфигов (YAML / Hydra) для запуска экспериментов. Каждый эксперимент должен описываться файлом: модель, оптимизатор, seed-молекула, целевое свойство, гиперпараметры.

---

## Фаза 1 — Evaluation framework

> Фреймворк оценки нужно построить **до** экспериментов, чтобы все модели и оптимизаторы оценивались единообразно.

### 1.1 Метрики генерации молекул

Реализовать модуль `evaluation/metrics.py` с расчётом стандартных метрик:

| Метрика | Что измеряет |
|---|---|
| **Validity** | % декодированных SMILES, прошедших RDKit-парсинг |
| **Uniqueness** | % уникальных среди валидных |
| **Novelty** | % не встречающихся в обучающей выборке |
| **Diversity** | средний внутри-выборочный Tanimoto distance |
| **Reconstruction accuracy** | % точно восстановленных из латентного вектора |

### 1.2 Метрики оптимизации свойств

Реализовать метрики, специфичные для задачи оптимизации:

| Метрика | Что измеряет |
|---|---|
| **Property improvement** | Δ целевого свойства (logP, QED, SA) относительно seed |
| **Success rate** | % молекул с улучшением > порога |
| **Similarity to seed** | Tanimoto (Morgan FP) между seed и оптимизированной молекулой |
| **Pareto efficiency** | доля решений на Pareto-фронте (improvement vs. similarity) |

### 1.3 Визуализация и отчёты

Скрипт / notebook для автоматической генерации сравнительных таблиц и графиков:
- Таблица `модель × оптимизатор × метрика`
- Scatter-plot: property improvement vs. structural similarity
- Латентное пространство (t-SNE / UMAP), раскрашенное по свойству
- Pareto-фронт для multi-objective случая

---

## Фаза 2 — Первая модель (SMILES-VAE)

### 2.1 Интеграция chemical_vae

Склонировать `aspuru-guzik-group/chemical_vae`, адаптировать зависимости к текущему окружению. Обернуть в единый интерфейс `BaseVAE` (encode, decode, sample, reconstruct) чтобы все модели имели одинаковый API.

### 2.2 Обучение / загрузка pretrained

Воспроизвести обучение на ZINC250K либо загрузить pretrained чекпоинт. Проверить reconstruction accuracy и validity на тестовой выборке. Зафиксировать baseline-метрики из фазы 1.

### 2.3 Property predictor

Обучить MLP-регрессор, предсказывающий целевое свойство (logP) по латентному вектору. Этот предиктор будет использоваться gradient-based оптимизатором и как surrogate для BO.

### 2.4 Gradient optimization

Реализовать gradient ascent в латентном пространстве: взять seed → encode → итеративно шагать по градиенту property predictor → decode. Оценить результаты через evaluation framework.

---

## Фаза 3 — Библиотека оптимизаторов

### 3.1 Bayesian Optimization

Реализовать BO поверх латентного пространства (GP surrogate + acquisition function). Использовать BoTorch или аналог. Подключить к тому же SMILES-VAE, сравнить с gradient ascent.

### 3.2 Evolutionary strategies (CMA-ES)

Реализовать CMA-ES в латентном пространстве. Fitness = значение целевого свойства декодированной молекулы. Провести эксперименты, добавить в таблицу сравнения.

### 3.3 Сравнительный анализ оптимизаторов (на SMILES-VAE)

Собрать результаты всех трёх оптимизаторов на одной модели. Построить таблицы и графики из фазы 1.3. Зафиксировать выводы: какой метод лучше по каким метрикам и при каких условиях.

---

## Фаза 4 — Вторая и третья модели

### 4.1 JT-VAE

Интегрировать `wengong-jin/icml18-jtnn`. Обернуть в `BaseVAE`. Обучить / загрузить pretrained. Прогнать все 3 оптимизатора, собрать метрики.

### 4.2 SELFIES-VAE

Интегрировать SELFIES-представление (`aspuru-guzik-group/selfies`). Обучить VAE на SELFIES-строках (можно адаптировать архитектуру SMILES-VAE). Прогнать оптимизаторы, собрать метрики.

### 4.3 (Опционально) HierVAE / MoLeR

Если позволяет время — добавить одну из более сложных архитектур. Тот же протокол: обернуть → обучить/загрузить → прогнать оптимизаторы → метрики.

### 4.4 Кросс-модельное сравнение

Матрица `модель × оптимизатор` по всем метрикам. Ответить на вопросы:
- Какое представление (SMILES / JT / SELFIES) даёт лучшее латентное пространство для оптимизации?
- Зависит ли лучший оптимизатор от выбора модели?
- Где trade-off между validity и property improvement?

---

## Фаза 5 — Собственные улучшения

> Конкретные направления определяются по итогам фаз 3-4. Ниже — кандидаты.

### 5.1 Multi-objective оптимизация

Одновременная оптимизация нескольких свойств (например, QED + SA + similarity). Построение Pareto-фронта. Сравнить с single-objective подходами.

### 5.2 Constrained optimization

Оптимизация с ограничением: сохранять молекулярный scaffold или подструктуру seed-молекулы. Реализовать как штраф или как hard constraint.

### 5.3 Uncertainty-aware navigation

Использовать неопределённость property predictor (ensemble / MC dropout) для того, чтобы оставаться в регионах латентного пространства с надёжными предсказаниями.

### 5.4 Собственная модификация VAE

На основе выводов — предложить изменения в архитектуре или loss-функции (например, property-conditioned prior, disentangled latent space, weighted ELBO).

---

## Фаза 6 — Финализация

### 6.1 Воспроизводимость

Все эксперименты запускаются из конфигов одной командой. Фиксация random seeds. Dockerfile или environment.yml для окружения.

### 6.2 Документация

README с описанием проекта, инструкциями по запуску, таблицей результатов. Docstrings в ключевых модулях.

### 6.3 Итоговый отчёт

Собрать все результаты в единый документ / notebook: мотивация → методология → эксперименты → результаты → выводы.
